{
  "name": "@backend-jobs/worker",
  "version": "1.0.0",
  "description": "Worker service for backend jobs system",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@backend-jobs/shared": "^1.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
